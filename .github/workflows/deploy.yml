name: "Global Mesh Deployment"

on:
  push:
    branches:
      - main
      - feature/full-stack-api

jobs:
  terraform-ansible:
    name: "Infrastructure & Config"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.0

      - name: Terraform Init & Apply
        id: tf_apply
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          cd terraform
          terraform init
          terraform apply -auto-approve
          
          # This command extracts ONLY the IP address and ignores all warnings/pipes
          RAW_IP=$(terraform output -json instance_public_ip | jq -r '.')
          echo "NEW_IP=$RAW_IP" >> $GITHUB_ENV

      - name: Wait for SSH to be ready
        run: |
          echo "Waiting for instance at ${{ env.NEW_IP }} to boot..."
          sleep 30

      - name: Run Ansible Setup
        env:
          ANSIBLE_HOST_KEY_CHECKING: "False"
        run: |
          # 1. Create the key at the root directory from GitHub Secrets
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ./final-key
          chmod 400 ./final-key
          
          # 2. Run Ansible, passing the NEW_IP dynamically 
          ansible-playbook -i ansible/inventory.ini ansible/setup.yml \
            --extra-vars "TARGET_IP=${{ env.NEW_IP }}" \
            --private-key ./final-key \
            -u ubuntu

  build-and-push:
    name: "Build and Push"
    needs: terraform-ansible # Recommended: Ensure infra is up before building
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          # Note: Ensure these secrets are named DOCKER_USERNAME and DOCKER_PASSWORD in GitHub
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push API Image
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          # Note: This will tag the image as 'nath2025/mesh-api:latest'
          tags: ${{ secrets.DOCKER_USERNAME }}/mesh-api:latest